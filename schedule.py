import schedule
from config import TELEGRAM_TOKEN

def schedule_wobmat_pic:
    pass
    #schedule.every().day.at("08:00").do(get_random_image_path())

def schedule_wobmat_fact:
    pass
    #schedule.every().day.at("08:00").do(get_random_wobmat_fact())